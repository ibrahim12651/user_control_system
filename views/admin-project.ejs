<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= projeler.project_name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script></head>
<body>

  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Stajyer</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mobileactive" aria-controls="mobileactive" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="mobileactive">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/dashboard">Anasayfa</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Projeler(Github)</a>
          </li>
            <li class="nav-item">
                <a class="nav-link" href="/iletisim">İletişim</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/logout">Çıkış Yap</a>
            </li>
        </ul>
        <form action="/" class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Arama" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Ara</button>
        </form>
      </div>
    </div>
  </nav>

<div class="container mt-5 float-end">
  <h3>> Proje Adı : <%= projeler.find(project => project.project_name).project_name %></h3>
  <input type="hidden" name="project_name" value="<%= projeler.find(project => project.project_name).project_name %>">
  <p><span style="font-weight: bold;">> Proje Hakkında : </span><%= projeler.find(project => project.project_name).project_about %></p>
  <div class="row">
    <% if (files.length > 0) { %>
      <div class="col-md-6">
        <h2 class="mb-3">Dosyalar</h2>
        <ul class="list-group">
          <% files.forEach(file => { %>
            <li class="list-group-item">
            <a href="/admin/dosya/<%= projeler.find(project => project.project_name).project_name %>/<%= file %>"><%= file %></a> <a href="/admin/dosya/delete/<%= projeler.find(project => project.project_name).project_name %>/<%= file %>" class="btn btn-danger btn-sm float-end">Sil</a><a href="/admin/dosya/download/<%= projeler.find(project => project.project_name).project_name %>/<%= file %>" class="btn btn-success btn-sm float-end">İndir</a>
            </li>
          <% }); %>
        </ul>
      </div>
    <% } %>


</body>
</html>